import React from 'react'
import ReactDOM from 'react-dom/client'
import SprachFlow from './SprachFlow.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SprachFlow />
  </React.StrictMode>,
)
